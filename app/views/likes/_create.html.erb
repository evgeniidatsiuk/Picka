<%= form_with url: image_likes_path(@image), local: true do |form| %>
  <button style="border: none; background: none" type="submit" value="submit">
      <%= image_tag("like.png", size: "40") %>
      </button>
<% end %>
