Nickname:
<%= @user.nickname%>
<%= image_tag @user.avatar.url, style:"width:400px;"%>
<% if current_user.id == @user.id %>
  <%= link_to "Setting", edit_user_registration_path %>
  <%= render 'images/new'%>
<%end%>

Photos:
<%= Image.where(user_id: @user.id).count %>

<%= render 'relationships/form' %>

<br>
<%= link_to "followers", followers_user_path(@user) %>
<%=link_to "following", following_user_path(@user) %>
<br>

<%= render @user.images%>
